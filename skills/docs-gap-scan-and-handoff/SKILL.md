---
name: docs-gap-scan-and-handoff
description: Use when explicitly asked to scan a repository for missing superpowers docs or missing AGENTS.md and CLAUDE.md doc indexes.
---

# Docs Gap Scan And Handoff

## Overview

This skill scans and repairs missing `superpowers` documentation. Core
principle: the main agent identifies module boundaries first, then dispatches
at least four module-scoped subagents; only the main agent may write shared
instruction files or resolve cross-module gaps after all subagents return.

This skill is explicit-only. Do not trigger it implicitly.

## When to Use

- The user explicitly asks to run this skill
- The repository may be missing `superpowers` docs generated by the workflow
- `AGENTS.md` or `CLAUDE.md` may be missing module-level doc indexes
- The repository may need a repair pass instead of a read-only audit
- The expected result includes both repair work and a final main-agent check

Do not use this for general README or API docs. Do not use it as a default
replacement for broader audit skills unless the user explicitly names it.

## Scope

Stay scoped to these surfaces only:

- `docs/superpowers/specs/<module>/`
- `docs/superpowers/plans/<module>/`
- `docs/superpowers/commits/<module>/`
- `AGENTS.md`
- `CLAUDE.md`

Repairs may include:

- missing module directories
- missing spec, plan, or commit docs
- invalid or legacy flat doc paths
- broken or missing spec/plan/commit cross-links
- missing instruction-file directory indexes

Default doc types remain `feat`, `fix`, `docs`, and `misc`.

## Rules

1. Before the real audit begins, the main agent may do only minimal discovery:
   repository root, instruction files, current `docs/superpowers/` layout, and
   module directory boundaries used for subagent assignment.
2. Dispatch at least four subagents whenever at least four modules exist. If
   there are more than four modules, keep assigning by module so ownership
   stays non-overlapping.
3. Module ownership must be exclusive for the current run. One module may be
   owned by only one subagent.
4. Main-agent strong wait is mandatory after dispatch. Do not redo module audit
   work while subagents are running.
5. Each subagent may scan and write only inside the `docs/superpowers/`
   directories for its assigned module.
6. Subagents must not edit `AGENTS.md` or `CLAUDE.md`.
7. Any required `AGENTS.md` or `CLAUDE.md` update must be returned to the main
   agent as a handoff package. The main agent performs the actual write.
8. Fix deterministic issues directly inside subagent-owned module surfaces:
   - create missing module directories
   - create minimum usable docs
   - normalize legacy flat paths
   - repair broken cross-links
   - normalize invalid type segments
9. If a subagent discovers a gap in another module, it must not repair that
   other module directly. Return the cross-module gap to the main agent with
   evidence instead.
10. The main agent must not process cross-module gaps immediately when one
   subagent reports them. Wait until all subagents have returned, then resolve
   cross-module work in one consolidation step.
11. Do not invent business facts. If repository evidence is insufficient, create
   only the minimum usable skeleton and state the gap explicitly.
12. Instruction-file indexes must list directories only, never document files.
13. Treat upstream flat examples such as `docs/superpowers/specs/...` as
    legacy and normalize them to module paths before repairing.
14. The workflow does not complete when subagents finish. The main agent must:
    - collect all subagent outputs
    - consolidate cross-module gaps after all subagents return
    - write shared instruction files
    - verify subagent-owned changes
    - verify shared-file writes
    - report the final checked result

## Agent Responsibilities

### Main Agent

Owns:

- minimal repository discovery
- module detection and module-to-subagent assignment
- dispatching at least four subagents when module count allows
- strong wait
- cross-module gap consolidation after all subagents finish
- writing `AGENTS.md`
- writing `CLAUDE.md`
- final verification

### Module Subagents

Owns:

- scanning assigned module docs
- repairing missing docs for the assigned module
- normalizing legacy paths and filenames inside the assigned module
- repairing spec/plan/commit links inside the assigned module
- preparing shared-index handoff content
- returning cross-module gap reports when another module is implicated

Each subagent may inspect `AGENTS.md` and `CLAUDE.md` read-only in order to
prepare the handoff package, but it must not write either file. A subagent may
reference another module as evidence, but it must not write that module's docs.

## Handoff Contract

When a subagent finds a required shared-file change, it must return:

- target file path
- suggested index block text
- module directories covered by that block
- short evidence note explaining why the block is needed

When a subagent finds a cross-module gap, it must also return:

- source module
- target module
- missing doc type or missing link type
- short evidence note
- suggested related path or reference path

Returning only file paths is insufficient. Returning a patch is optional, but
the main agent still performs the actual shared-file write and all cross-module
consolidation.

## Repair Depth

Use minimum truthful content unless the repository already defines something
stricter.

| Situation | Default action |
|-----------|----------------|
| Missing module directory | Create it |
| Missing linked doc with clear purpose | Create minimum usable doc |
| Missing linked doc without enough evidence | Create skeleton with explicit gap note |
| Missing shared index block | Return a directory-level block for main-agent write |
| Invalid type segment | Normalize to `feat` / `fix` / `docs` / `misc` |
| Legacy flat path | Move or rewrite to module path |

## Final Verification

The main agent must verify all of the following before closing the task:

- module ownership was assigned before the real audit work
- at least four subagents were used when at least four modules existed
- no module was owned by more than one subagent
- `AGENTS.md` and `CLAUDE.md` were written only by the main agent
- subagent-owned docs were actually created or repaired
- no subagent wrote another module's docs
- cross-module gaps were handled only after all subagents returned
- directory indexes contain directory paths only
- spec, plan, and commit links resolve cleanly after repair

## Quick Reference

| Concern | Policy |
|---------|--------|
| Trigger mode | Explicit only |
| Main agent before dispatch | Minimal discovery + module detection only |
| Main agent after dispatch | Strong wait |
| Subagent count | At least 4 when module count allows |
| Subagent writable surface | Assigned module only |
| Shared files | `AGENTS.md`, `CLAUDE.md` |
| Shared-file writer | Main agent only |
| Shared-file handoff | Path + suggested block + covered dirs + evidence |
| Cross-module handoff | Source module + target module + gap type + evidence + related path |
| Missing docs | Minimum truthful content |

## Common Mistakes

- Letting the main agent perform the audit before dispatch
- Letting multiple subagents own the same module
- Letting a subagent write `AGENTS.md` or `CLAUDE.md`
- Letting a subagent repair another module instead of handing the gap back
- Handling a cross-module gap as soon as one subagent reports it
- Returning only a path list for shared-file changes
- Treating subagent completion as final completion
- Writing file-level entries into the instruction-file index
- Inventing missing facts instead of leaving explicit gap notes
